<?php

require_once "src/rutas.php";
$router->despachar();


/**
if (isset($_GET['pagina'])) {
    $pagina = $_GET['pagina'];
} else {
    $pagina = 'sesion';
}

require "src/controlador/" . $pagina . ".php";
 */
